import feint from '../../../assets/Feint.png'
import { Jobs, JobSkills } from '../../../styles'

const Samurai = () => {
  return (
    <Jobs>
      <p>SAM</p>
      <JobSkills src={feint} alt="Feint" />
    </Jobs>
  )
}

export default Samurai
